<% if user_signed_in? %>
  <!-- .to_s(:nice_date) 
  <%= content_for :date_title do %>
    Rotina &ndash; <%= I18n.l(Date.today, format: :long) %>
  <% end %>
  <h3>
    <% if content_for(:date_title) %>
      <%= yield :date_title %>
    <% else %>
      Minha Rotina
    <% end %>
  </h3>
  <br>-->
  
    <div class="row">
      <div class="col btn-group">
        <%= link_to '+ Atividade', new_activity_path, class: "btn btn-primary" %>
        <%= link_to '+ Emoção', new_emotion_path, class: "btn btn-primary" %>
        <%= link_to '+ Categoria', new_category_path, class: "btn btn-secondary" %>
        <!-- <%= link_to 'Novo Hábito', new_activity_path, class: "btn btn-secondary" %>
        <% current_user.categories.each do |cat| %>
          <% if cat.title == 'thought' || cat.title == 'habit' || cat.title == 'action' %>
            <%= link_to ('Adicionar ' + t(cat.title)), new_activity_path(category: cat.title), class: "btn btn-secondary" %>
          <% end %>
        <% end %>-->
      </div>
    </div>
     <!-- <div class="col text-center"><%= link_to 'Nova Categoria', new_category_path, class: "btn btn-secondary" %></div> 
    </div>-->
 <!-- <% @activities.each do |activity| %>
    <% if activity.user == current_user %>
      <% end %>
  <% end %> 
  [{x: activity.start_time.strftime('%Y-%m-%d %H:%M:%S'), y: TimeDifference.between(activity.start_time, activity.end_time).in_minutes.to_i}]
  <button class="btn btn-primary" id="calendarCollapser" type="button" data-toggle="collapse" data-target="#collapseCalendar" aria-expanded="false" aria-controls="collapseCalendar">
    Calendário
  </button>
  <div class="collapse" id="collapseCalendar">
      <br>-->
  <br>  
  <div id="tooltip" role="tooltip">t<div id="arrow" data-popper-arrow></div></div>
  <div id="calendar" style="width: 90%;" class="mx-auto"></div>
  <br><br>
  <div class="btn btn-group btn-scondary">
    <div class="btn btn-secondary" onClick='var chart'></div>
    <%= link_to '7 dias', chart_period_path(7), class: "btn-sml btn-secondary" %>
    <%= link_to '30 dias', chart_period_path(30), class: "btn-sml btn-secondary" %>
    <%= link_to 'Ano', chart_period_path(365), class: "btn-sml btn-secondary" %>
  </div>
  <%= puts @masteries, Date.today.days_ago(@chart_period) %>
  <%= line_chart [
    {name: "Masteria", data: @masteries.filter{ |k,v| k > DateTime.now.days_ago(@chart_period) }},
    {name: "Prazer", data: @pleasures},
    {name: "Escala de emoção", data: @emotion_scales}
  ] %>
  <br>
  <!--
  <table class="table table-dark table-striped table-hover">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Categoria</th>
        <th>Início</th>
        <th>Fim</th>
        <th>Masteria</th>
        <th>Prazer</th>
        <th>Comentário</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @activities.each do |activity| %>
        <% if activity.user == current_user %>
          <tr>
            <td><%= link_to activity.name, activity, class: "btn btn-secondary btn-sm"%>
            <td><%= activity.category.title.capitalize %></td>
            <td><%= activity.start_time.to_formatted_s(:time) if activity.start_time? %></td>
            <td><%= activity.end_time.to_formatted_s(:time) if activity.end_time? %></td>
            <td><%= activity.mastery %></td>
            <td><%= activity.pleasure %></td>
            <td><%= activity.comment %></td>
            <td>
                <%= link_to 'Deletar', activity, method: :delete, data: { confirm: 'Tem certeza?' }, class: "btn btn-secondary btn-sm" %>
            </td> 
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>-->

  <br>
  <div class="row justify-content-center">
    <%= link_to 'Ver todas as atividades', archives_index_path, class: "btn btn-secondary btn-lg" %>
  </div>
  <br><br>

<% else %>
  <h1>Bem-vindo ao Rout</h1>
  <p>Aqui você pode registrar sua rotina e acompanhar seu desenvolvimento pessoal!</p>
<% end %>
